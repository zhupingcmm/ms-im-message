# JWT 共享密钥：im-user / im-connect / im-gateway 三者必须用同一值
# 生成方法：openssl rand -hex 32
apiVersion: v1
kind: Secret
metadata:
  name: im-jwt-secret
  namespace: im
type: Opaque
stringData:
  IM_JWT_SECRET: "REPLACE_ME_AT_LEAST_32_BYTES_RANDOM"
